- name : conditons
  connection : local
  hosts : local 
  vars :
    number : 9
  tasks : 
  - name : check number is less than 10 or not
    ansible.builtin.debug:
      msg : " Given number {{ number }} is less 10 "
    when: number < 10


