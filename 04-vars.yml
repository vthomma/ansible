- name : play level variables
  hosts: local 
  connection : local  #Ansible will connect to localhost
  vars:
    COURSE : Ansible
    DURATION : 10 HRS
    MENTOR : USER

  tasks:
     - name : print the course  info 
       ansible.builtin.debug:
        msg: "COURSE info COURSE : {{ COURSE }},DURATION : {{ DURATION }}, MENTOR : {{ MENTOR }}"

